# Session Record - 2026-03-12 15:45

## 本次目标

- 把现有单流程 Demo 演进为更清晰的 multi-agent 框架
- 落地 JD PDF Agent 和 PostgreSQL 持久化

## 本次完成内容

- 将 JD 持久化策略写回需求文档
- 接入 PostgreSQL 并创建本地开发数据库 `resume_agent`
- 新增 SQLAlchemy 数据层和 JD repository
- 实现独立 `JD PDF Agent`
- 将原流程拆分为：
  - `Resume Parser Agent`
  - `Matching Agent`
  - `Ranking Agent`
  - `Explanation Agent`
- 更新后端接口：
  - `GET /api/presets`
  - `POST /api/jds`
  - `POST /api/jds/upload-pdf`
  - `POST /api/match`
- 更新前端：
  - 支持上传 JD PDF
  - 支持确认并保存 JD 草稿
  - 从数据库读取 JD 列表
  - 支持勾选已入库 JD 后发起匹配
  - 展示更详细的匹配原因

## 关键修改文件

- `backend/app/main.py`
- `backend/app/db.py`
- `backend/app/db_models.py`
- `backend/app/repositories/jd_repository.py`
- `backend/app/workflows/jd_pdf_graph.py`
- `backend/app/workflows/resume_graph.py`
- `backend/app/workflows/match_graph.py`
- `frontend/components/match-app.tsx`
- `frontend/lib/types.ts`
- `docs/requirements.md`

## 验证结果

- 后端测试通过：`7 passed`
- 前端 `npm run build` 通过
- PostgreSQL 本地库已创建：`resume_agent`
- JD 持久化和 JD PDF 解析链路已接通

## 遇到的问题

- 启动钩子在测试里不足以保证数据库初始化，已改为显式初始化
- JD 确认保存时有覆盖原始解析元数据的风险，已修复

## 下一步建议

- 增加手工构造的 JD/简历回归样本
- 继续优化 scoring，使岗位区分度更稳定
- 逐步引入数据库迁移工具，而不是只靠 `create_all`

---

## 追加更新 - 2026-03-12 16:15

### 本次追加目标

- 为当前 multi-agent 项目生成一批本地可复用的测试简历和 JD PDF

### 本次追加完成内容

- 新增脚本 `scripts/generate_sample_pdfs.py`
- 生成 5 份中文简历 PDF
- 生成 5 份中文 JD PDF
- 将样本生成方式补充到运行文档

### 生成结果

简历样本目录：

- `backend/data/sample_pdfs/resumes/`

JD 样本目录：

- `backend/data/sample_pdfs/jds/`

### 文件示例

- `resume_python_backend_zh.pdf`
- `resume_ai_engineer_zh.pdf`
- `jd_python_backend_zh.pdf`
- `jd_ai_application_engineer_zh.pdf`

### 下一步建议

- 用这批样本做手工回归
- 逐步增加“高匹配 / 中匹配 / 低匹配”对照样本
- 后续可再加扫描版 PDF 测试样本

---

## 追加更新 - 2026-03-12 20:35

### 本次追加目标

- 优化前端信息架构
- 把单页操作流拆成更清晰的多页面跳转

### 本次追加完成内容

- 新增统一导航壳层 `AppShell`
- 将前端入口拆分为：
  - 首页 `/`
  - JD 管理 `/jds`
  - 匹配工作台 `/match`
  - Agents 说明 `/agents`
- 将 JD PDF 上传、草稿确认、岗位库展示独立到 `JD 管理` 页面
- 将简历上传、岗位选择、排序结果、trace 查看独立到 `匹配工作台` 页面
- 将首页改为面向演示的产品入口页，而不是直接堆叠所有表单
- 生产构建验证通过，确认路由、样式和类型定义可用

### 关键修改文件

- `frontend/app/layout.tsx`
- `frontend/app/page.tsx`
- `frontend/app/jds/page.tsx`
- `frontend/app/match/page.tsx`
- `frontend/app/agents/page.tsx`
- `frontend/components/app-shell.tsx`
- `frontend/components/jd-manager.tsx`
- `frontend/components/match-workspace.tsx`
- `frontend/app/page.module.css`

### 验证结果

- 前端 `npm run build` 通过
- 生成静态页面：
  - `/`
  - `/jds`
  - `/match`
  - `/agents`

### 下一步建议

- 继续提升视觉层次和结果可视化，而不再回到单页堆叠结构
- 为 `/jds` 和 `/match` 补更强的空状态和引导文案
- 视演示需要继续增加更有辨识度的图表和结果对比模块

---

## 追加更新 - 2026-03-12 21:05

### 本次追加目标

- 缩短匹配等待时间
- 让匹配进行中的状态更明确、更像真实产品

### 本次追加完成内容

- 使用异步并行方式处理岗位级打分
- 使用异步并行方式处理 Explanation Agent 的结果说明生成
- 在匹配日志中补充并行度摘要，便于后续观察性能
- 为 `/match` 页面增加动态加载卡片
- 加入阶段式文案：
  - 简历解析
  - 并行打分
  - 解释生成
- 在匹配发起时清空旧结果，避免用户误以为旧结果仍在更新

### 关键修改文件

- `backend/app/workflows/match_graph.py`
- `backend/app/main.py`
- `backend/app/config.py`
- `frontend/components/match-workspace.tsx`
- `frontend/components/match-workspace.module.css`

### 验证结果

- 后端测试通过：`7 passed`
- 前端 `npm run build` 通过
- `/match` 页面静态构建成功，新增加载动画未破坏类型和路由

### 下一步建议

- 用更多 JD 数量做一次人工性能对比，确认并行优化的体感提升
- 如果模型调用成为主要瓶颈，再考虑加入可配置并发上限和超时策略
- 后续可把加载阶段和后端 trace 更紧密联动，而不是仅做前端估计式展示

---

## 追加更新 - 2026-03-12 21:18

### 本次追加目标

- 为下一步 `Tailwind CSS + MUI` 前端重构准备风格拍板清单

### 本次追加完成内容

- 新增前端风格决策文档：
  - `docs/decision-checklists/frontend-style-requirements.md`
- 将需要用户确认的内容拆成可填写清单，包括：
  - 整体视觉方向
  - 色彩与明暗模式
  - 首页 / JD 管理页 / 匹配页风格
  - 动画强度
  - MUI 使用范围
  - 图表与组件气质偏好

### 下一步建议

- 用户先填写这份前端风格清单
- 再基于统一风格一次性替换现有 CSS Modules 方案，避免边做边改

---

## 追加更新 - 2026-03-12 21:26

### 本次追加目标

- 在风格方向已经确认后，再补一份前端实现层拍板清单

### 本次追加完成内容

- 新增文档：
  - `docs/decision-checklists/frontend-implementation-requirements.md`
- 将剩余影响前端落地的问题收敛为少量实现项，包括：
  - 首页是否单独布局
  - `/match` 页重点呈现方式
  - 深浅色切换策略
  - 图标库和图表库选择
  - 结果详情展开策略
  - 移动端适配目标
  - 品牌命名展示

### 下一步建议

- 用户只需补这份实现层清单
- 如果不想细答，可直接采用文档里的默认建议后开始重构

---

## 追加更新 - 2026-03-12 21:48

### 本次追加目标

- 按已确认风格把前端重构为 `Tailwind CSS + MUI`
- 从旧的 CSS Modules 风格切换到统一设计系统

### 本次追加完成内容

- 接入前端依赖：
  - `tailwindcss`
  - `@mui/material`
  - `next-themes`
  - `lucide-react`
  - `recharts`
- 新增全局 provider 和 MUI 主题系统，支持浅色 / 深色模式
- 将首页重构为产品介绍页
- 将内部页重构为左侧导航工作台结构
- 重写 `/jds` 页面，使其更像内容管理后台
- 重写 `/match` 页面，使其突出：
  - 排名
  - 总分
  - 推荐结论
  - 单个轻量图表
- 保留并升级匹配中的动态加载反馈
- 清理旧的 CSS Modules 和废弃组件，避免前端存在两套样式系统并存

### 关键修改文件

- `frontend/package.json`
- `frontend/postcss.config.mjs`
- `frontend/app/globals.css`
- `frontend/app/layout.tsx`
- `frontend/app/page.tsx`
- `frontend/app/agents/page.tsx`
- `frontend/components/providers.tsx`
- `frontend/components/app-shell.tsx`
- `frontend/components/jd-manager.tsx`
- `frontend/components/match-workspace.tsx`

### 验证结果

- `npm install` 完成
- `npm run build` 通过
- 构建期间遇到一次旧 `.next` 产物缓存问题，清理后恢复正常

### 下一步建议

- 继续压缩 `/match` 页的首屏复杂度，必要时再拆卡片层级
- 如果后续要进一步提升演示效果，可补品牌文案和参考站点风格
- 视模型返回长度再调整结果卡片的信息密度

---

## 追加更新 - 2026-03-12 22:02

### 本次追加目标

- 解释异常岗位来源
- 为 JD 管理页补“手动新增 / 删除岗位”能力

### 本次追加完成内容

- 排查出 `陈可 | 产品经理` 不是预置 JD，而是一条 `jd_pdf` 持久化记录
- 确认该记录内容更像简历摘要而不是岗位描述
- 新增后端删除接口：
  - `DELETE /api/jds/{job_id}`
- 新增前端能力：
  - 手动新增岗位
  - 删除非预置岗位
- 清理本地数据库中的异常记录 `8c8b3eca6d564da5bc6d602faf17af09`

### 关键修改文件

- `backend/app/main.py`
- `backend/app/repositories/jd_repository.py`
- `backend/tests/test_api.py`
- `frontend/components/jd-manager.tsx`

### 验证结果

- 后端测试通过：`9 passed`
- 前端 `npm run build` 通过

### 下一步建议

- 后续可考虑在 JD 草稿确认前增加更强的“这看起来像简历而不是 JD”的校验
- 如需更稳，可在删除前增加确认弹窗

---

## 追加更新 - 2026-03-12 22:12

### 本次追加目标

- 去掉匹配页里的岗位手动选择
- 修正 Top 5 总分图的岗位名显示问题

### 本次追加完成内容

- `/match` 页面改为默认匹配当前全部岗位
- 删除“参与匹配岗位勾选区”，减少用户操作
- 将结果总结图改成横向条形图，避免长岗位名在 X 轴上显示不全
- 将 Top 5 总分图放到结果概览摘要区域，并补充 Top 1 提示

### 关键修改文件

- `frontend/components/match-workspace.tsx`
- `README.md`
- `docs/project-guide/run-project.md`
- `docs/current-state.md`

### 验证结果

- 前端 `npm run build` 通过

### 下一步建议

- 如果后续岗位数量继续增加，可把 Top 5 改成可切换 Top 5 / Top 10
- 如需进一步压缩页面高度，可把运行追踪折叠到独立抽屉或二级面板

---

## 追加更新 - 2026-03-12 22:24

### 本次追加目标

- 提升匹配结果的行动价值，而不只是显示“合适 / 不合适原因”

### 本次追加完成内容

- 后端新增结构化解释字段：
  - `follow_up_questions`
  - `action_recommendation`
- 将 Explanation 结构模型化，减少前后端对自由 dict 的依赖
- 解释生成逻辑升级为：
  - 合适的原因
  - 风险提示
  - 建议追问
  - 建议动作
- 前端结果卡片同步增加“建议追问”和“建议动作”展示区
- 修复解释模型升级后 `match_graph` 中的兼容问题

### 关键修改文件

- `backend/app/models.py`
- `backend/app/services/explainer.py`
- `backend/app/workflows/match_graph.py`
- `backend/tests/test_api.py`
- `frontend/lib/types.ts`
- `frontend/components/match-workspace.tsx`

### 验证结果

- 后端测试通过：`9 passed`
- 前端 `npm run build` 通过

### 下一步建议

- 如果后续还想继续增强价值，可把“建议动作”分成更明确的推进等级
- 也可以为不同岗位生成更有针对性的追问模板，而不是仅基于缺口通用生成

---

## 追加更新 - 2026-03-12 22:34

### 本次追加目标

- 修复匹配结果里重复 key 的前端报错
- 改善第一次打开和第一次跳转到 `/match` 时的体感速度

### 本次追加完成内容

- 修复结果列表中重复使用文本作为 key 的问题
- 对“合适原因 / 风险提示 / 建议追问”统一改成稳定复合 key
- 将 `recharts` 图表从匹配工作台主组件中拆出并改成动态加载
- 将 `/match` 页面本体也改成动态导入，并增加独立 loading 骨架
- 首次进入 `/match` 时先渲染轻量骨架，再异步加载完整工作台

### 关键修改文件

- `frontend/components/match-workspace.tsx`
- `frontend/components/match-summary-chart.tsx`
- `frontend/components/workspace-loading.tsx`
- `frontend/app/match/page.tsx`
- `frontend/app/match/loading.tsx`

### 验证结果

- 前端 `npm run build` 通过
- `/match` 路由首屏体积从约 `125 kB` 降到约 `12.1 kB`

### 下一步建议

- 如果还想继续压缩，可以把结果详情区域继续拆成懒加载子模块
- 如需更平滑的跳转，可再给侧边导航增加更明确的 hover / active 状态过渡

---

## 追加更新 - 2026-03-12 22:46

### 本次追加目标

- 允许在 JD 管理页删除原始默认岗位
- 修正并优化“手动新增岗位”区域的整体展示

### 本次追加完成内容

- 后端删除接口放开对 `preset` 类型 JD 的限制
- 调整测试，允许删除预置岗位后再恢复默认记录
- 手动新增岗位改成更清晰的分组表单：
  - 基础信息
  - 匹配条件
- 为输入框补充了更明确的占位说明和 helper text
- 增加“清空表单”操作
- JD 列表中的所有岗位现在都可删除，包括预置岗位

### 关键修改文件

- `backend/app/main.py`
- `backend/tests/test_api.py`
- `frontend/components/jd-manager.tsx`

### 验证结果

- 后端测试通过：`9 passed`
- 前端 `npm run build` 通过

### 下一步建议

- 如果后续担心误删，可增加删除确认弹窗
- 如需进一步提升录入体验，可补岗位模板快速填充

---

## 追加更新 - 2026-03-12 23:00

### 本次追加目标

- 审查并修复深色模式下刺眼、发灰的模块配色

### 本次追加完成内容

- 在全局样式中补全一组更细的主题 token：
  - `surface-muted`
  - `surface-overlay`
  - `surface-overlay-strong`
  - `accent-soft`
  - `accent-soft-2`
  - `accent-border`
  - `accent-tint`
- 调整深色模式下的 surface 和 overlay，使卡片不再发灰发白
- 将多个页面中的硬编码浅色背景替换为主题 token：
  - 首页
  - Agents 页
  - JD 管理页
  - 匹配工作台
  - 侧边导航壳层
- 图表颜色也切到主题 token，避免深色模式下出现不协调的亮灰条

### 关键修改文件

- `frontend/app/globals.css`
- `frontend/components/providers.tsx`
- `frontend/components/app-shell.tsx`
- `frontend/components/jd-manager.tsx`
- `frontend/components/match-workspace.tsx`
- `frontend/components/match-summary-chart.tsx`
- `frontend/app/page.tsx`
- `frontend/app/agents/page.tsx`

### 验证结果

- 前端 `npm run build` 通过

### 下一步建议

- 如果还要继续精修视觉，可逐页截图检查深浅两套主题的层级对比
- 后续尽量继续基于 token 配色，不要再回到局部硬编码 `rgba(255,255,255,...)`

---

## 追加更新 - 2026-03-12 23:18

### 本次追加目标

- 继续处理第一次打开和第一次切换页面时体感偏慢的问题

### 本次追加完成内容

- 新增前端岗位列表缓存：
  - `frontend/lib/job-cache.ts`
- `/jds` 和 `/match` 现在会优先读取 sessionStorage 中的岗位缓存，再后台刷新
- 为 `/jds` 补充动态导入和 loading 骨架
- 继续保留 `/match` 的动态导入和 loading 骨架
- 这样页面首次切换时不必总是重新等待 `/api/presets`

### 关键修改文件

- `frontend/lib/job-cache.ts`
- `frontend/components/jd-manager.tsx`
- `frontend/components/match-workspace.tsx`
- `frontend/components/jd-manager-loading.tsx`
- `frontend/app/jds/page.tsx`
- `frontend/app/jds/loading.tsx`

### 验证结果

- 前端 `npm run build` 通过
- `/match` 路由体积进一步降到约 `9.13 kB`

### 下一步建议

- 如果还要继续降切换等待，可考虑把首页就主动预热 `/jds` 和 `/match` 的数据
- 如果后续 JD 数据规模变大，可给缓存增加时间戳和失效策略

---

## 追加更新 - 2026-03-12 23:28

### 本次追加目标

- 为“多简历 + 多岗位匹配”功能准备拍板清单

### 本次追加完成内容

- 新增文档：
  - `docs/decision-checklists/batch-matching-requirements.md`
- 将批量匹配实现前需要确认的问题整理成清单，包括：
  - 核心批量场景
  - 输入规模上限
  - 输出结构
  - 异步还是同步
  - 进度反馈
  - 失败处理
  - 是否持久化
  - 是否引入简历入库
  - UI 入口
  - 批量调度层的 Agent / Workflow 设计

### 下一步建议

- 用户先填写这份批量匹配清单
- 然后再决定是先做“最小可用批量版”还是直接做完整矩阵视图

---

## 追加更新 - 2026-03-12 23:45

### 本次追加目标

- 在 `/match` 恢复“默认全量匹配 + 可选手动选择岗位”的交互

### 本次追加完成内容

- 更新匹配工作台：
  - 默认仍匹配全部岗位
  - 新增 `全部岗位 / 手动选择` 切换
  - 手动模式支持岗位搜索
  - 手动模式支持 `source_type` 筛选：
    - 预置
    - 上传 JD
    - 手动新增
  - 支持 `全选当前筛选结果`
  - 支持 `清空选择`
  - 支持已选岗位 chip 展示和单个移除
- 提交匹配请求时：
  - 全量模式提交全部岗位 ID
  - 手动模式只提交已选岗位 ID
  - 手动模式下若未选择岗位会给出错误提示
- 顶部概览卡同步反映当前匹配方式和匹配范围

### 关键修改文件

- `frontend/components/match-workspace.tsx`
- `README.md`
- `docs/project-guide/run-project.md`
- `docs/current-state.md`
- `docs/backlog.md`

### 验证结果

- 前端 `npm run build` 通过

### 下一步建议

- 后续做批量匹配时，优先复用这套搜索和来源筛选交互，不要再重新发明一套岗位选择器

---

## 追加更新 - 2026-03-13 00:18

### 本次追加目标

- 实现“多份简历 + 多岗位”的批量匹配能力

### 本次追加完成内容

- 后端新增批量匹配接口：
  - `POST /api/batch-match`
  - `GET /api/batch-match/{task_id}`
- 新增批量任务持久化表和简历记录表：
  - `batch_match_tasks`
  - `resumes`
- 批量任务当前采用：
  - FastAPI `BackgroundTasks`
  - 任务状态轮询
  - 本地文件保存原始简历 PDF
  - PostgreSQL 保存任务状态、简历元数据、结构化结果
- 新增批量任务编排服务：
  - 逐份简历解析
  - 逐份简历调用现有匹配流程
  - 聚合出“每份简历 Top 3 岗位”
  - 聚合出“每个岗位 Top 3 候选人”
- 前端新增 `/batch-match` 页面
- 批量页支持：
  - 上传多份简历 PDF
  - 默认匹配全部岗位
  - 切换到手动选择岗位
  - 搜索岗位
  - 按来源筛选岗位
  - 查看任务进度
  - 切换简历视角 / 岗位视角

### 关键修改文件

- `backend/app/main.py`
- `backend/app/models.py`
- `backend/app/db_models.py`
- `backend/app/repositories/batch_repository.py`
- `backend/app/services/batch_match_service.py`
- `backend/tests/test_api.py`
- `frontend/app/batch-match/page.tsx`
- `frontend/app/batch-match/loading.tsx`
- `frontend/components/batch-match-workspace.tsx`
- `frontend/components/app-shell.tsx`
- `frontend/lib/types.ts`

### 验证结果

- 后端 `pytest` 通过：`10 passed`
- 前端 `npm run build` 通过

### 当前限制

- 批量任务仍是 demo 级实现，依赖当前运行中的应用进程
- 如果服务重启，进行中的任务不会自动恢复
- 当前结果页优先展示 Top 3，不是完整矩阵分析页

### 下一步建议

- 如果后续要做长任务或更大批量，优先把批量执行迁移到独立 worker
- 如需更强分析能力，再追加矩阵视图、结果导出和失败重试

---

## 追加更新 - 2026-03-13 00:42

### 本次追加目标

- 增加“快速匹配 / 全面匹配”两种运行模式，用 explanation 调用量换速度

### 本次追加完成内容

- 后端为单简历匹配和批量匹配都增加 `match_mode`
- 支持两种模式：
  - `fast`
    - 规则打分仍覆盖全部岗位
    - Explanation Agent 只对 Top 3 结果生成完整 explanation
    - 其余岗位保留规则摘要
  - `full`
    - 所有匹配岗位都生成完整 explanation
- 批量任务表新增 `match_mode`
- 为现有数据库增加运行时兼容处理：
  - 如果已有 `batch_match_tasks` 表，会补 `match_mode` 字段
- 前端 `/match` 和 `/batch-match` 页面都新增模式切换
- 批量结果详情弹层在快速模式下会明确提示：
  - 当前只有快速摘要
  - 如需全部详细 explanation，应切到全面匹配

### 关键修改文件

- `backend/app/main.py`
- `backend/app/models.py`
- `backend/app/db_models.py`
- `backend/app/repositories/batch_repository.py`
- `backend/app/workflows/match_graph.py`
- `backend/app/services/batch_match_service.py`
- `backend/tests/test_api.py`
- `frontend/components/match-workspace.tsx`
- `frontend/components/batch-match-workspace.tsx`
- `frontend/lib/types.ts`

### 验证结果

- 后端 `pytest` 通过：`11 passed`
- 前端 `npm run build` 通过

---

## 追加更新 - 2026-03-13 01:24

### 本次追加目标

- 优化解释、风险与建议部分，避免同一技能缺口或同一亮点在多个句子里重复出现

### 本次追加完成内容

- 在 explanation 层新增关键词去重逻辑
- 合适原因现在会跳过已经出现过的匹配关键词
- 风险提示现在会优先展示硬性要求失败，并避免同一组缺失项在技能、项目等多个维度里重复出现
- 建议追问也会按缺失项去重，不再围绕同一技能反复追问
- 新增单测覆盖重复案例：
  - `数据分析` 不应在优势里重复出现多次
  - `产品设计 / 用户研究` 不应在风险里以不同维度重复刷屏

### 关键修改文件

- `backend/app/services/explainer.py`
- `backend/tests/test_explainer.py`
- `docs/current-state.md`
- `docs/backlog.md`

### 验证结果

- 后端 `pytest` 通过：`12 passed`

---

## 追加更新 - 2026-03-13 01:34

### 本次追加目标

- 将“合适的原因 / 风险提示 / 建议追问”改成由大模型生成，并作为前端主展示内容

### 本次追加完成内容

- explanation 层改成：
  - 规则先产出结构化证据
  - 再把证据和约束喂给大模型
  - 大模型只返回结构化 JSON：
    - `fit_reasons`
    - `risk_reasons`
    - `follow_up_questions`
- 解析器支持 fenced JSON
- 如果大模型输出不合法或调用失败，自动回退到规则生成版本
- 前端主展示继续聚焦这三块内容
- “建议动作”不再作为结果页核心展示区域

### 关键修改文件

- `backend/app/services/explainer.py`
- `backend/tests/test_explainer.py`
- `frontend/components/match-workspace.tsx`
- `frontend/components/batch-match-workspace.tsx`
- `docs/current-state.md`
- `docs/backlog.md`

### 验证结果

- 后端 `pytest` 通过：`13 passed`
- 前端 `npm run build` 通过

---

## 追加更新 - 2026-03-13 01:43

### 本次追加目标

- 把“建议下一步动作”加回 explanation 和前端展示，例如建议推进面试

### 本次追加完成内容

- 结构化 explanation JSON 新增：
  - `action_recommendation`
- 大模型现在会一起生成：
  - `fit_reasons`
  - `risk_reasons`
  - `follow_up_questions`
  - `action_recommendation`
- 如果模型没有给出动作建议，仍然会回退到规则生成版本
- 单简历匹配页和批量详情页都重新展示：
  - `建议下一步动作`

### 关键修改文件

- `backend/app/services/explainer.py`
- `backend/tests/test_explainer.py`
- `frontend/components/match-workspace.tsx`
- `frontend/components/batch-match-workspace.tsx`

### 验证结果

- 后端 `pytest` 通过：`13 passed`
- 前端 `npm run build` 通过

### 下一步建议

- 如果还要继续提速，下一步优先做“批量任务里的简历级并发”
- 如果还要继续提高清晰度，可在结果卡片上更明显地区分“完整 explanation”和“快速摘要”

---

## 追加更新 - 2026-03-13 00:56

### 本次追加目标

- 完成批量任务里的简历级并发

### 本次追加完成内容

- 批量任务从“逐份简历串行处理”改成“受控简历并发”
- 新增配置项：
  - `BATCH_RESUME_PARALLELISM`
- 当前批量任务会同时并发多份简历，每份简历内部仍复用已有的匹配流程
- 并发处理后仍保留：
  - 任务状态轮询
  - 已完成数量进度
  - 简历顺序稳定输出
  - 失败项记录
- 运行时 trace 现在会记录：
  - 当前简历并发上限

### 关键修改文件

- `backend/app/config.py`
- `.env.example`
- `backend/app/services/batch_match_service.py`
- `README.md`
- `docs/project-guide/run-project.md`
- `docs/current-state.md`
- `docs/backlog.md`

### 验证结果

- 后端 `pytest` 通过：`11 passed`
- 前端 `npm run build` 通过

### 下一步建议

- 如果还要继续提升大批量场景的速度，下一步应该做独立 worker，而不是继续单纯放大并发参数

---

## 追加更新 - 2026-03-13 01:08

### 本次追加目标

- 修正维度分数条的显示逻辑，不再统一按固定上限长度显示

### 本次追加完成内容

- 新增共享组件：
  - `frontend/components/dimension-score-list.tsx`
- 单简历匹配页和批量结果详情弹层都改成：
  - 以“当前这组维度分数中的最大值”作为 100%
  - 低分项不再因为固定满分基准而显得过短
- 保留每个维度的文字说明和数值 chip

### 关键修改文件

- `frontend/components/dimension-score-list.tsx`
- `frontend/components/match-workspace.tsx`
- `frontend/components/batch-match-workspace.tsx`

### 验证结果

- 前端 `npm run build` 通过

### 下一步建议

- 如果后续还想增强可读性，可以进一步把不同维度的理论权重上限也显式标出来，避免相对长度和绝对权重被混淆

---

## 追加更新 - 2026-03-13 01:15

### 本次追加目标

- 修正维度分数条的长度基准，按各自理论满分显示

### 本次追加完成内容

- 在后端 `DimensionScore` 中新增 `max_score`
- 各维度现在会带上自己的理论满分：
  - 技能匹配 `35`
  - 工作年限 `20`
  - 项目经历 `20`
  - 行业经验 `10`
  - 教育背景 `5`
  - 加分项匹配 `10`
- 前端分数条不再按“当前最大值”或统一的 `35` 显示
- 现在每条分数条都会按 `score / max_score` 渲染
- 数值标签也改成 `实际分数/理论满分`

### 关键修改文件

- `backend/app/models.py`
- `backend/app/services/scoring.py`
- `frontend/lib/types.ts`
- `frontend/components/dimension-score-list.tsx`

### 验证结果

- 后端 `pytest` 通过：`11 passed`
- 前端 `npm run build` 通过
